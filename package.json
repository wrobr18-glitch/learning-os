{
  "name": "learning-os-monorepo",
  "private": true,
  "engines": {
    "node": ">=20.0.0 <22.0.0",
    "npm": ">=9.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  },
  "packageManager": "npm@10.9.2",
  "dependencies": {
    "pdf-parse": "^2.4.5"
  }
}
